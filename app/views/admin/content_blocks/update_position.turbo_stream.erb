<%= turbo_stream.update dom_id(@container_block, :content_blocks) do %>
  <%=
    render @container_block.content_blocks.ordered,
    container_block: @container_block,
    project: @project
  %>
<% end %>